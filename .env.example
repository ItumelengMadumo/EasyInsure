VITE_APP_ENV=dev
# Used only by `npm run outputs:dev`; never expose secrets through VITE_ variables.
AMPLIFY_APP_ID=
# Server-side CLI inputs for `npm run personas:seed:dev`; do not prefix with VITE_.
EASYINSURE_ENV=dev
EASYINSURE_DEV_USER_POOL_ID=
EASYINSURE_PERSONA_SECRET_ID=easyinsure/dev/persona-credentials
